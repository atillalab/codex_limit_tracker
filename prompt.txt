Extract these 5 fields from the `/status` output:
	1) Weekly reset date
	2) Remaining weekly context percentage
	3) Days until weekly reset
	4) Daily context usage budget percentage
	5) Remaining weekly context after one day budget

Return only valid JSON in this exact shape:
	{"weekly_reset_date":"YYYY-MM-DD","weekly_context_left_percent":number,"days_until_weekly_reset":number,"daily_context_budget_percent":number,"weekly_context_after_today_budget_percent":number}

Rules:
	- Convert the reset date to YYYY-MM-DD.
	- Return percentage fields as numbers (no % sign).
	- Compute `days_until_weekly_reset` as:
	  (weekly_reset_date - current_date) + 1
	  where `current_date` is today's date in YYYY-MM-DD.
	- Use whole calendar days (ignore time-of-day).
	- Compute `daily_context_budget_percent` as:
	  weekly_context_left_percent / days_until_weekly_reset
	- Compute `weekly_context_after_today_budget_percent` as:
	  weekly_context_left_percent - daily_context_budget_percent
	- If any required input field is missing, set dependent computed fields to null.
	- If `days_until_weekly_reset` is 0 or negative, set `daily_context_budget_percent` and
	`weekly_context_after_today_budget_percent` to null.
	- No extra text.

/status output:
[PASTE HERE]
